module.exports = {
    rules: {
        // don't allow deleting variables
        'no-delete-var': 'error',

        // don't allow undefined variables
        'no-undef': 'error',

        // don't allow use of `undefined`
        'no-undefined': 'error',

        // don't allow undefined variables
        'no-unused-vars': 'error',

        // don't allow using a variable before it's defined
        'no-use-before-define': 'error',
    },
};

